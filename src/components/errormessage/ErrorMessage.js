import React from "react";

function ErrorMessage() {

    return (
        <div className="error--container">
            <p className="error--container--message">
                We're having an issue. Please refresh the page; It could take several tries - we're sorry for the inconvenience :(
            </p>
        </div>
    );
}

export default ErrorMessage;